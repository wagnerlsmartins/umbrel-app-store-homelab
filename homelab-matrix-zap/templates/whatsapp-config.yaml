# Template mínimo do mautrix-whatsapp para o Matrix Zap.
# O bridge completa as chaves ausentes com os defaults dele no primeiro boot
# (upgrade automático de config) e troca os "generate" por tokens aleatórios
# ao gerar o registration. Nada aqui é segredo.
homeserver:
  address: http://homelab-matrix-zap_synapse_1:8008
  domain: matrix.wagnermartins.in

appservice:
  address: http://homelab-matrix-zap_whatsapp_1:29318
  hostname: 0.0.0.0
  port: 29318
  id: whatsapp
  bot:
    username: whatsappbot
    displayname: Ponte WhatsApp
  as_token: generate
  hs_token: generate

database:
  type: sqlite3-fk-wal
  uri: file:/data/mautrix-whatsapp.db?_txlock=immediate

bridge:
  command_prefix: "!wa"
  permissions:
    "*": relay
    "matrix.wagnermartins.in": user
    "@wagner:matrix.wagnermartins.in": admin
